[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: marks tests that require live database or external services (deselect with '-m "not integration"')
